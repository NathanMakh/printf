#ifndef MAIN_H
#define MAIN_H
#include <stdarg.h>
#include <unistd.h>
#include <stdio.h>
#include <string.h>

int _putnum(unsigned int num, unsigned int base);
int _printf(const char *format, ...);
int _putchar(char c);
int _putnum(int num)

#endif
